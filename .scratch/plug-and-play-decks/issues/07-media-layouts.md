# 07 — Media layouts

**What to build:** `plate` (one image alone), `bleed` (one video, plays on enter, pauses on leave, the video component moves into the theme), `link` (one centred clickable link) and `photo` (background image with text placed in the clear area via `place:`).

**Blocked by:** 01

**Status:** done

- [x] Each has a demo slide in markdown only
- [x] The video pauses when leaving its slide; a missing asset shows the fallback rather than breaking the deck
- [x] `check` passes on the demo
