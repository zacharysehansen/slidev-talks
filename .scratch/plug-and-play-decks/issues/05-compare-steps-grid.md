# 05 — Comparison and grid layouts

**What to build:** `compare` (two columns via `::right::`, `###` column heads, `>` quotes, `::foot::` spanning line), `steps` (numbered list, `hero: N` highlights one item) and `grid` (`###` column heads each followed by a short list or a count).

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] Demo slides reproduce the look of the temporal deck's era comparison, four questions and specialty grid, in markdown only
- [ ] `check` passes on the demo
