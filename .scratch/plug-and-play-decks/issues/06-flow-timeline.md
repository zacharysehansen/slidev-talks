# 06 — Flow and timeline layouts

**What to build:** `flow` (a list, one item per step, `direction: down | across`, a final `=` item as the outcome) and `timeline` (a list of `year: label`, with `mark:` drawing in last, finished state in print mode).

**Blocked by:** 01

**Status:** done

- [x] Demo slides reproduce the stage flow, pipeline, validation gates, LoRA and Cecil's editions slides in markdown only
- [x] The timeline mark animates in dev and is present in PDF export
- [x] `check` passes on the demo
