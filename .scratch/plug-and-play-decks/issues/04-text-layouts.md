# 04 — Text layouts

**What to build:** `title`/`close`, `statement`, `default`, `quote`, `divider` and `number` layouts, each filled from plain markdown with no HTML. Shared conventions: `*em*` in Bloom, `**strong**` in Arroyo, `field: light` flips any layout to the light field. See DESIGN_PLAN.md §5.

**Blocked by:** 01

**Status:** done

- [x] Each layout has a demo slide on both fields, written in markdown only
- [x] `check` passes on the demo
